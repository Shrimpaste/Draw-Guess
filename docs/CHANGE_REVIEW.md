# 分批修复审查记录

## 第一批：对局可靠性

分支：codex/01-game-reliability。

- 修复：内存会话作为认证依据、60 秒宽限和多标签页集合；单房间约束和幂等加入；成员/角色/阶段/roundId 校验；guessId 裁定与有界消息；实时 schema、64 KiB 限制、速率限制、心跳与协议错误清理；大厅广播；非 401 网络错误重试；默认管理员接口关闭；投稿数据隔离；测试库隔离。
- 验证：npm test 21 项通过（服务端 17、客户端 4）；npm run build 通过；git diff --check 通过。
- 人工审查：检查认证来源、离房/注销通知、旧回合请求、多个 socket 清理、异步 effect 清理、HTTP/WS 状态版本和审核数据边界。
- 审查修正：离房后 pending 答案标记关闭；处理非法 upgrade URL；客户端忽略过期版本和旧身份的请求结果；集成测试等待服务端异步解绑完成。
- 结论：本批范围通过。画笔增量/预算、计时和布局留在后续批次，不作为本批已修复项。

## 第二批：绘画与回合

分支：codex/02-drawing-and-rounds。

- 修复：50 ms 增量绘画与分块；稳定笔迹 ID / 偏移 / 画布版本；撤销、橡皮、点绘与指针取消；离屏缓冲；画布容量明确拒绝并回传快照；服务端 30 秒出题 / 100 秒作画时限；主动结束和离人结束；结算保留画作与分数变化。
- 验证：npm test 32 项通过（服务端 23、客户端 9），npm run build 通过。真实 WebSocket 测试验证抬笔前同步、清空后旧消息拒绝；假时钟验证截止时间和计分；指针测试覆盖取消、角色切换和点绘。
- 人工审查：逐项检查消息顺序、版本间隙恢复、计时器释放、撤销后的迟到分块、预算与快照恢复、绘制缓冲生命周期。修正首块发送失败后残留的刷新定时器。
- 结论：本批范围通过。布局与错误文案在第三批完善。

## 第三批：游戏体验

分支：codex/03-game-experience。

- 重组登录、大厅、对局页；保留纸色 / 墨色 / 橙色。画布与猜词记录并排，手机上下排列；玩家积分折叠。移除装饰性长文和依赖网络的字体加载。
- 添加中文错误映射、失败保留输入、离线禁用操作、回合结果、房间码复制、原生 dialog 焦点管理 / Escape 关闭、投稿去重与审核入口。审核密钥不持久化；普通接口只提供已审核词包元数据，管理接口不存在资源返回 404。
- 回归：猜词失败重试、断线禁用、指定答案 ID 裁定、结果展示、投稿重复词与失败保留、API 投稿/审核/删除。完整测试 36 项通过，构建与 diff 检查通过。
- 浏览器：本地隔离内存数据库，验证登录、创建房间、朋友加入、开局、猜错记录、投稿、审核通过、Escape 关闭和焦点返回。1280×720 画布顶部约 252px、输入框顶部约 625px；390×844 画布顶部约 272px、输入框顶部约 719px，无横向溢出。截图见 screenshots/room-desktop.png 与 room-mobile.png。
- 审查修正：按视窗高度约束桌面画布，倒计时不超过本阶段上限，连接错误可关闭。人工检查已覆盖接口兼容与组件生命周期。本轮未在真实手机硬件上验证触笔。

## 第四批：上线准备

分支：codex/04-vps-readiness。

- 兼容依赖更新：ws 8.21.3、nanoid 5.1.16、Express 4.22.3、Vite 6.4.3；Vitest 更新到 4.1.11 以修补 mocker 问题。npm 官方审计从 12 项降至 0 项。
- 默认监听回环地址、精确来源校验、仅信任 loopback 代理；数据库目录自动创建、显式外键、移除未使用玩家持久化代码；优雅关闭、服务语法检查、Node 24 CI。
- 独立 systemd 应用 / HTTPS 代理、持久化词库与证书、每日在线备份、完整性检查和回滚说明。备份回归验证 WAL 数据、一致性及拒绝覆盖。
- 本地 37 项测试通过，构建通过；10 客户端、200 分块 / 3200 点、1 次重连、结算一致，0 错误；P95 1.32 ms，为本机结果而非 VPS 指标。
- 人工审查：运行用户与读写目录、Caddy IP 证书 issuer、Node 真实路径（避免 nvm 被 ProtectHome 隐藏）、代理 IP 信任、备份与旧 WAL 恢复边界。Caddy 2.11.4 官方 SHA-512 校验与配置验证通过。部署后的外网证书和网络验证另行记录。

VPS 安装审查修正：better-sqlite3 11.10.0 转源码编译时发现缺少 g++；更新至官方发布的 13.0.3（N-API，内置平台二进制）。本地 37 项回归、构建、10 客户端验证通过，审计仍为 0。增加可在 SSH 断开后保留结果的 release 验证脚本。

公网浏览器审查修正（2026-09-16）：撤销后立即绘画会携带旧 epoch，被服务器拒绝。现等待撤销 / 清空的权威快照后恢复作画，显示同步状态；发送失败、错误快照、切换回合或失去绘画权限会解除等待。颜色和线宽仍可预选。新增三项回归，先复现旧行为再修复；40 项测试（服务端 25、客户端 15）、构建、diff 检查通过。人工核对 WebSocket 顺序、快照版本和断线恢复路径。

最终验收：Debian 全新 npm ci、40 项测试、构建通过；隔离十人 P95 1.74 ms，服务器实际 HTTPS 代理十人 P95 3.20 ms。本机公网十人 P95 1259.42 ms，均 0 错误，验证重连 / 3200 点一致性 / 计分结算。浏览器确认可信 HTTPS、撤销等待和恢复、桌面 / 手机无横向溢出、超时保留画作、重启后重新登录；控制台无警告或错误。截图 public-desktop.png / public-mobile.png。

运维验收：应用和代理启用开机启动；服务正常退出重启，自动重启计数 0；每日备份定时器已自动触发且完整性检查成功。证书首次续期尚未发生；恢复 / 回滚步骤已审查，未在生产词库执行灾难恢复演练。GitHub Actions 配置已提交，尚未远程推送触发。结论：本批可合并；公网路径延迟及实际手机触摸体验继续通过小规模朋友试玩收集。
