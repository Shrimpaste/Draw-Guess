# 修复依据

## 延迟测量 2026-09-17

- 本机线路经过 Meta TUN，不能据此判断出站为代理或直连；未改用户网络设置。
- 服务器健康请求约 1 ms；公网 WS RTT 中位数 165.7 ms，三人笔画 P95 191.76 ms；本机相同流程 P95 1.02 ms。
- 猜词触发完整画布广播及 HTTP 响应。3200 点快照约 106 KB，笔画分块约 719 字节。采用显式协商紧凑同步，旧客户端保持完整快照。
- 本地绘制已即时反馈；50 ms 发送周期可降低，但须避开每秒 60 条限制。撤销保留 epoch 确认，暂不引入额外队列与回滚复杂度。

完整初始证据见 REVIEW-2026-09-15.md。

- 初始 HEAD 与 origin/main 均为 7d858fe；已有 4 个 UI 源文件修改及 4 个未跟踪文档，必须独立保存。
- 初始测试仅 4 项，服务端测试会重置默认 SQLite 玩家表。此后测试必须强制独立数据库。
- 核心风险：WebSocket 未捕获 error、非成员猜词、多房间残留、新旧 socket 解绑、SQLite 与内存会话不一致、pendingGuess 覆盖、画笔抬起才同步和静默丢弃。
- Review Changes 图谱工具当前不可用，审查使用源码 diff、回归测试、真实本地 HTTP/WS 与浏览器。
- 部署建议为单域名单进程；VPS SSH 别名/域名/已有服务情况已向用户询问，继续不依赖它们的本地工作。

## 已验证的部署事实

- 服务器 Debian 12 / 4 GB，既有 8080 / 8090 服务保留；后端只监听 127.0.0.1:3001，Caddy 独立提供 80 / 443。
- 无域名方案已落地：Let's Encrypt shortlived 公网 IP 证书 + Caddy 自动续期，不需要安装自签证书。
- 真实公网延迟暴露了撤销与新笔画的竞态；修复后等待权威快照，避免旧 epoch 新笔画被拒绝。
- 十人同步的服务器处理和代理开销较低；本机经现有网络路径访问延迟较大，后续应使用朋友的实际网络再体验，不能用服务器内测延迟代表公网体验。

## UI 体验审阅（2026-09-16，已完成）

- 代码证据：手机 swatch 27×32px、滑杆 44px；工具条 flex-wrap 使清空落单。猜词 onSubmit 通过 App 全局 busy，导致输入和其他请求控件一起 disabled。顶端 feedback 参与普通文档流，会推动整个对局布局。
- 角色共用完整工具条与输入表单，非画师也看到整排 disabled 工具。结果只更新回合栏与聊天文字，分数变化在默认收起的 details 内。复制状态无定时恢复，复制失败无具体反馈。
- Dialog 基于原生 showModal，已有基础模态语义与 Escape / 焦点返回；不可将它描述为完全没有焦点管理。缺少的是一致的进入 / 退出、初始焦点和内容滚动设计，以及脏表单离开策略。
- 官网初查：Radix 可直接配现有 CSS、渐进引入交互基础件；shadcn 官方方案需 Tailwind，组件源码可定制；Mantine 提供完整样式 / Provider 和样式覆盖。后续比较应计入迁移、视觉定制与状态流程成本，不能只看演示按钮。
- shadcn 当前官方文档提供 Radix 路径与 JavaScript 配置；采用它不要求把业务改成 TypeScript。Base UI 当前同样覆盖 Toolbar / Dialog / Drawer / Toast 等，是可行的单一底层备选，不能将其描述为功能不全。
- 线上大厅浏览器确认当前是普通表单 + 原生 radio / details；房间卡没有头像、等待状态强调或入场反馈。当前存在其他玩家房间，审阅不进入其房间。
- 线上投稿弹窗实测：初始焦点在“关闭”按钮；输入临时名称，Escape 关闭再打开，输入被清空。未提交任何词包。弹窗已有基础模态能力，但有意外丢草稿和开始输入多走一步的问题。
- 官方 shadcn Dialog 浏览器实测：名称输入获得初始焦点；标题 / 描述 / 字段 / 底部操作层次分明，关闭入口与主操作分开。它提供可复用基础，但不会自动解决本项目的草稿策略、连续猜词和回合反馈。
- 方案倾向：按需采用 shadcn 的 Radix 版本 + Tailwind 4 用于共享 UI 层；保留 JSX、业务组件和游戏状态，统一暖纸主题。主要代价是旧全局 button/input/label/dialog 规则与 Tailwind Preflight / cascade layer 的迁移；第一批必须验证，不能直接运行生成器覆盖 styles.css。
