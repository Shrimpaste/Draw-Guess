# Draw-and-Guess 开发日志

日期：2026-04-11

## 今日完成

- 搭建了全栈项目基础结构：
  - `client`：React + Vite
  - `server`：Express + WebSocket + SQLite
- 完成了临时 ID 登录、在线唯一 ID 处理、房间创建 / 加入 / 离开、两种模式的基础回合逻辑。
- 实现了实时画布同步、房间大厅、词库投稿与审核接口。
- 完成了房间专注视图：
  - 进入房间后切换到独立房间页面
  - 离开房间后返回大厅
- 修复了刷新导致身份和房间状态立即丢失的问题：
  - 前端不再在刷新时主动删除会话
  - 后端对 WebSocket 断开增加短暂保留窗口，允许刷新后快速重连恢复
- 对大厅与房间视图做了一轮结构和交互优化。
- 已完成本地验证：
  - `npm run test --workspace server`
  - `npm run test --workspace client`
  - `npm test`
  - `npm run build`

## 当前状态判断

当前项目已经具备“可运行、可联通、核心玩法可走通”的产品原型基础，但前端仍未达到稳定产品水位，尤其在“回合反馈、状态感知、交互层次、视觉精修”上还有明显提升空间。

## 当前已知问题

### 用户明确提出的问题

1. 结算与错误信息不够显眼。
2. 玩家提交的答案应单独列出为 chat / 气泡弹出，并保留为历史记录。
3. 需要继续做更美观的视觉与交互优化。

### 自行 Review 发现的问题

#### 交互与表现

- 房间大厅虽然比之前清晰，但仍然偏“静态卡片展示”，节奏和记忆点不够。
- 房间页面仍偏功能面板化，回合开始、猜中、判定正确、回合结束这些关键事件的反馈还不够强。
- 提示系统目前仍以基础 banner 为主，缺少统一的 toast / notice / round-status 机制。

#### 回合体验

- 玩家猜词主要依赖输入框提交，实时反馈和历史浏览仍不够直观。
- 猜测记录虽已有消息流雏形，但尚未形成成熟的“聊天区 / 气泡流”体验。
- 判定局中，“待裁定答案”和“历史猜测记录”还应进一步分区并高亮最新项。

#### 断线与状态恢复

- 当前刷新恢复依赖短暂断线保留，适合原型阶段，但不是长期稳定方案。
- 如果浏览器断开时间较长，仍可能丢失当前临时会话与房间状态。
- 房间核心状态仍存于内存，服务端重启后会丢失。

#### 工程与数据层

- 项目此前出现过部分中文编码污染，关键文件已重写，但后续提交仍需严格保持 UTF-8。
- 目前缺少更完整的前端交互测试，以及断线重连/恢复相关测试。
- 管理员审核词库目前只有接口，没有独立后台页面。

## 部署建议

### GitHub Pages

- GitHub Pages 只适合部署 `client` 构建后的静态资源，不适合直接部署当前 `Express + WebSocket + SQLite` 后端。
- 如果后续要上 GitHub Pages，前端需要改成通过环境变量连接远程 API 与 WebSocket 服务，例如：
  - `VITE_API_BASE_URL`
  - `VITE_WS_BASE_URL`
- 同时需要补充 GitHub Pages 的 `base` 配置和对应的 Actions 自动部署流程。

### 后端免费托管建议

- 优先推荐 `Railway`
  - 对当前 `Node.js + WebSocket` 结构更友好
  - 支持较直观的服务部署
  - 相比纯静态平台，更适合当前项目演进
- 次选 `Render`
  - 上手简单
  - 但免费实例可能休眠，实时体验会受影响
- 若后续愿意重构架构，可考虑：
  - `Cloudflare Workers + Durable Objects / D1`
  - 更适合实时房间逻辑，但需要明显调整现有后端实现

### 数据层建议

- 当前后端使用本地 SQLite 文件，若部署到无持久磁盘或临时文件系统的平台，会有数据丢失风险。
- 若继续追求免费且更稳定的线上数据层，可评估：
  - `Turso`
  - `Supabase Postgres`
- 如果短期只是做演示，也可以先接受轻量数据丢失风险，优先把演示链路跑通。

## 下一步计划

### 第一优先级：反馈与结算系统

- 实现统一的前端反馈机制：
  - 错误提示
  - 成功提示
  - 回合状态提示
  - 操作处理中提示
- 让关键事件更显眼：
  - 开始回合
  - 提交答案
  - 判定正确 / 错误
  - 猜中答案
  - 回合结束结算
- 增加结算展示层：
  - 本轮结果
  - 猜中玩家
  - 画师 / 给词者收益
  - 下一步操作入口

### 第二优先级：将猜词记录改造成独立聊天区

- 将玩家提交的答案单独列出为聊天流 / 气泡流。
- 保留历史猜测记录，并区分：
  - 系统消息
  - 玩家猜测
  - 判定结果
  - 回合事件
- 给最新猜测增加更明确的视觉强调。

### 第三优先级：继续提升视觉与动效

- 优化房间大厅：
  - 更好的空状态
  - 更强的房间状态可读性
  - 更自然的进入动效与 hover 反馈
- 优化房间专注页：
  - 回合头部更有舞台感
  - 画布区域更突出
  - 侧栏信息密度更合理
- 提升整体一致性：
  - 统一颜色层级
  - 统一按钮状态
  - 统一卡片与提示风格

### 第四优先级：增强恢复能力与部署适配

- 评估将房间状态持久化到数据库或独立状态层。
- 增强断线重连与房间恢复逻辑，而不是仅依赖短暂保留窗口。
- 为静态前端部署补齐环境变量配置、远程后端地址配置、GitHub Pages 构建与发布流程。

## 已知仍需继续修改的内容

- 结算信息显示仍不够突出。
- 错误反馈不够醒目，成功反馈也不够明确。
- 玩家答案尚未形成完整聊天区体验。
- 房间内交互节奏仍偏“功能面板”，游戏氛围还不够。
- 大厅虽然已分层，但完成度仍未达到理想产品水位。
- 断线恢复仍是原型级方案，需要进一步增强。
- 部署方案尚未正式落地，前后端分离部署适配仍待实施。

## 下次建议切入顺序

1. 先做统一反馈系统与回合结算层。
2. 再把猜词记录改造成独立聊天区。
3. 然后精修房间页视觉与关键动效。
4. 最后完成前后端分离部署适配与更强的状态恢复。
