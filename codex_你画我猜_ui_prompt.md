# Codex 提示词：为“你画我猜”网页小游戏设计并实现优雅、美观、交互动画流畅的高质量前端 UI

你现在是一个顶级前端设计师 + 资深交互工程师 + 能主动阅读项目上下文的实现型助手。请你在当前项目中，先阅读项目文件与现有实现，再基于项目实际功能与 API 完成一个**可直接运行、设计感强、动画流畅、细节精致**的“你画我猜”网页小游戏前端 UI 设计与实现。

## 核心目标

请为“你画我猜”网页小游戏制作一套 **优雅美观、具有记忆点、非模板化、交互动效自然流畅、可实际投入使用** 的前端界面。不是只做静态视觉稿，而是要输出**可运行、可集成、可落地**的真实代码。

你必须严格避免做出廉价、泛 AI 风格、千篇一律的界面。整体作品需要有明确审美方向、完整视觉语言、统一细节标准和高完成度。

## 第一优先级要求：先读项目，再动手

在你开始编码之前，必须先完成下面这些事情：

1. **遍历并阅读当前项目目录结构**。
2. 找出：
   - 前端入口文件
   - 路由结构
   - UI 相关组件
   - 状态管理方式
   - 样式方案（CSS / SCSS / Tailwind / CSS Modules / styled-components 等）
   - 构建工具与框架（React / Vue / Next / Vite / 原生 HTML 等）
   - 与游戏逻辑相关的已有页面、组件、hooks、stores、utils
   - 网络请求层、API 封装、socket 连接、实时通信逻辑
3. **主动阅读项目中的接口定义与调用方式**，包括但不限于：
   - REST API
   - WebSocket / Socket.IO
   - 房间系统
   - 玩家加入/退出
   - 回合状态
   - 出题 / 猜词 / 聊天 / 计时 / 计分 / 结算
   - 画布同步、笔刷状态、颜色、清空、撤销等功能
4. 如果项目中有 README、API 文档、注释、类型定义、mock 数据、OpenAPI 文件、后端调用代码，优先阅读并据此实现。
5. 如果发现项目已经有部分 UI，请不要推翻重做成完全不兼容的结构，而是**在现有技术栈与功能边界内完成高质量升级**。

## 输出前必须先做的分析

在真正开始改代码前，请你先在内部完成以下判断，并据此选择设计方案：

### 1. 产品定位
这是一个什么气质的“你画我猜”？
- 派对感、轻松、有趣
- 精致、现代、带一点游戏秀场感
- 年轻化但不幼稚
- 有竞技氛围但不压迫

### 2. 视觉方向
你必须选择一个**明确且大胆**的审美方向，并贯彻到底。可以参考但不限于：
- 现代轻游戏 UI
- 轻奢未来感
- 杂志式排版 + 游戏操作面板融合
- 精致玩具感 / toy-like
- 柔和但高级的彩色玻璃 / layered translucency
- 有氛围感的深色夜间派对房间风格

但无论选哪种方向，都必须满足：
- **不廉价**
- **不俗套**
- **不使用过度常见的 AI 审美套路**
- **有辨识度**

### 3. 差异化记忆点
你必须设计至少一个让人记住的视觉/交互亮点，例如：
- 房间进入时的高质量动效编排
- 计时器的戏剧化表现
- 猜对时的庆祝反馈
- 画笔工具栏的高级交互
- 排行榜/结算面板的层次动画
- 观战与当前回合玩家状态的动态切换

## 严格设计原则

请严格遵守以下前端设计原则，这些要求来自 skill 的核心精神：

### A. 审美必须有明确立场
不要做“差不多好看”的普通页面，要做“有完整观点”的页面。
- 要么克制精致
- 要么张扬有趣
- 但必须统一、明确、完整

### B. 避免陈词滥调式 AI 设计
严禁默认使用以下套路：
- 泛滥的 Inter / Arial / Roboto / 系统默认字体
- 紫蓝渐变 + 白底卡片 的陈旧 AI 风格
- 普通 SaaS 控制台式布局
- 毫无个性的圆角卡片堆叠
- 缺乏场景感的空洞页面

### C. 字体要有性格
请为界面选择更有气质的字体搭配：
- 标题字体要有辨识度与游戏气质
- 正文字体要清晰耐读但不平庸
- 中英文混排要尽量和谐
- 如果项目环境不适合引入在线字体，请至少在 font stack 中体现风格取向，并提供优雅降级

### D. 色彩要克制而有锋芒
- 建立统一的 CSS 变量/设计令牌体系
- 主色、强调色、成功色、警告色、危险色、背景层级都要系统化
- 颜色应服务于“回合状态、身份角色、操作反馈、胜负提示”这些真实游戏语义
- 不要平均分配颜色，要有主次与视觉重心

### E. 动效不是点缀，而是体验的一部分
动效必须服务于理解与情绪。
重点加强：
- 首屏加载 / 房间进入
- 玩家加入退出
- 当前出题者 / 猜题者状态切换
- 倒计时压力变化
- 发送消息与猜对反馈
- 回合结束结算
- 模态框、抽屉、toast、面板展开收起

动效要求：
- 自然、丝滑、具有层次
- 优先使用性能友好的实现
- 避免廉价的弹跳和无意义的炫技
- 动画时长、缓动曲线、延迟编排要统一

### F. 空间布局要有设计感
可适度使用：
- 非完全对称布局
- 分层叠加
- 轻微越界元素
- 具有节奏感的留白
- 更像游戏房间而非后台管理系统

### G. 背景与氛围必须被认真设计
不要只给一个纯色背景了事。
可以结合项目风格加入：
- 细腻渐变
- 纹理/噪点
- 半透明层
- 模糊玻璃
- 网格/微图案
- 发光/阴影层次
- 与“画板、房间、游戏秀场”相关的氛围元素

## 功能理解要求

你必须根据项目中已有逻辑与 API，识别并正确服务以下典型页面/模块（如项目中存在）：

1. **首页 / 大厅页**
   - 创建房间
   - 加入房间
   - 快速开始
   - 历史房间 / 最近昵称 / 规则简介

2. **房间页**
   - 房间信息
   - 玩家列表
   - 准备状态
   - 房主操作
   - 开始游戏
   - 房间聊天

3. **游戏进行页 / 核心对局区**
   - 画布区域
   - 工具栏（画笔、颜色、橡皮、清空、撤销等）
   - 倒计时
   - 当前回合信息
   - 猜词聊天区
   - 玩家分数榜
   - 当前轮到谁
   - 自己身份提示（我在画 / 我在猜 / 我在观战）

4. **结算页 / 回合结算弹层**
   - 正确答案
   - 本回合得分
   - 总分排行
   - 下一轮过渡
   - 胜利高亮反馈

5. **状态反馈系统**
   - loading
   - empty
   - reconnecting
   - network error
   - 房间失效
   - 人数不足
   - 游戏未开始 / 已结束

## 实现要求

### 1. 先复用现有项目栈
你必须优先适配当前项目技术栈，不要无端重构。
- 如果是 React，就在 React 范式内实现
- 如果是 Vue，就在 Vue 范式内实现
- 如果有 Tailwind，就优先沿用
- 如果已有组件库，就谨慎融合，而不是机械堆砌默认样式

### 2. 代码必须真实可运行
不允许只写概念代码或伪代码。
你输出的代码必须：
- 能在当前项目中运行
- 有清晰的组件边界
- 有良好的命名
- 有合理的状态处理
- 有必要的注释但不要过量
- 不破坏现有业务逻辑

### 3. 强化可维护性
请尽量建立：
- 统一设计 token
- 通用状态组件
- 通用按钮/面板/徽章/弹层风格
- 合理拆分的 UI 组件
- 动画常量与时间曲线统一管理

### 4. 重视响应式与适配
至少保证：
- 桌面端体验优秀
- 中等屏幕不崩
- 小屏设备有基本可用性
- 关键交互区域不遮挡、不重叠、不失控

### 5. 重视可访问性
请考虑：
- 足够的对比度
- 键盘可用性
- 焦点态可见
- 不完全依赖颜色表达状态
- 动效不过分影响使用

## 具体 UI 细节要求

请重点打磨以下细节：

### 首页 / 大厅
- 品牌标题要有记忆点
- 创建房间与加入房间操作要明显但不俗
- 卡片、输入框、按钮、提示文案形成统一气质
- 可以有轻量引导动画或欢迎区视觉编排

### 房间页
- 玩家列表不要像普通表格
- 房主、已准备、当前轮次、在线状态要有精致可识别的表达
- 聊天区要有舒服的信息密度
- 开始按钮与等待提示要富有节奏感

### 游戏主界面
- 画布必须是视觉核心
- 但聊天、玩家列表、计时器、得分区不能沦为边角废件
- 工具栏应有高级感和即时反馈
- 倒计时应兼具可读性与紧迫感
- 猜对消息要有强反馈但不打断整体流程
- 轮到自己作画时，应有明显、令人兴奋的状态强化

### 结算与反馈
- 不能只弹一个平庸对话框
- 要有“这一轮结束了”的情绪收束
- 排名变化、得分增长、答案揭晓要有层次
- 庆祝反馈要克制高级，避免土味特效

## 对 Codex 的执行要求

请按以下顺序工作：

1. 先扫描项目并总结：
   - 技术栈
   - 入口文件
   - 页面结构
   - 核心组件
   - API/实时通信方式
   - 当前 UI 存在的问题
2. 再提出你选择的视觉方向，并简述原因。
3. 然后给出你计划修改的文件列表。
4. 接着开始实际编码。
5. 完成后输出：
   - 改动说明
   - 关键设计决策
   - 运行方式
   - 后续还可优化的点

## 允许你主动优化的内容

在不破坏核心功能前提下，你可以主动补强：
- 视觉层级
- 空状态设计
- toast / notification 体系
- 统一按钮体系
- 统一表单样式
- 面板层级与阴影系统
- 页面过渡动画
- 更好的聊天气泡样式
- 更好的分数榜与身份标记
- 更好的 loading skeleton / suspense 体验

## 但禁止这些行为

- 不要脱离项目实际 API 胡乱造接口
- 不要忽略现有业务逻辑
- 不要只改颜色和圆角就说完成了
- 不要输出“示意代码”冒充成品
- 不要做成通用后台管理模板
- 不要做成千篇一律的 AI 生成风格
- 不要引入明显与项目栈冲突的大型依赖，除非确有必要并说明原因
- 不要为炫技牺牲性能与可用性

## 质量标准

请以“产品级交付”为标准验收你的结果。最终成品应当满足：

- 一眼看上去就比普通学生作业/模板站高级
- 视觉风格统一，且有明确个性
- 交互动效自然舒服
- 功能状态表达清晰
- 和项目现有功能/API真正接上
- 代码结构清楚，不是一次性拼凑

## 最终目标

请把这个“你画我猜”网页小游戏的前端，做成一个：
**真正能玩、真正好看、真正有氛围、真正流畅、真正不像 AI 模板拼出来的作品。**

如果项目中已有实现，请在其基础上升级到更成熟、更完整、更精致的版本；如果项目中只有基础骨架，请基于真实功能与 API 把 UI 体系补齐。

开始前请先阅读项目，再基于项目实际内容落地，不要跳过“理解项目”这一步。
