# 分批修复计划

## 目标与范围

沿用 React / Express / ws / SQLite，完成审阅报告的修复、测试、审查；每批在 codex/ 分支工作，清晰提交，审查通过后本地合并 main。用户已授权本地提交、合并和部署到 codex@170.106.190.20；尚未推送 GitHub。

## 批次

- [complete] 0. 保存已审阅的工作区基线与计划。
- [complete] 1. 可靠性：隔离测试数据库；会话/成员约束；回合权限；答案 ID；实时校验、错误处理、限流和大厅更新；客户端恢复。
- [complete] 2. 绘画与玩法：增量笔迹、撤销/橡皮、触摸、服务端计时、结束路径、保留画作与消息历史。
- [complete] 3. 交互与布局：紧凑对局页、猜词流、连接状态、字段与模态反馈、词包审核入口、桌面/手机验证。
- [complete] 4. 上线：兼容依赖升级、CI、VPS 配置、备份/恢复/回滚、10 客户端测试。已部署 https://170.106.190.20，可信 IP 证书，公网浏览器验收通过；详情见 docs/DEPLOYMENT.md。
- [complete] 5. UI 体验方案：源码与线上控件审阅、官方组件方案比较、分批路线与验收标准已完成。方案见 docs/UI-EXPERIENCE-PLAN.md；推荐 shadcn/ui 按需引入 + 游戏组件定制，实施尚未开始。
- [complete] 6A. UI 组件与样板：shadcn / Radix、暖纸主题、控件状态、开发预览、CSS 隔离与体积验证。
- [complete] 6B. 核心交互：工具条、局部提交、IME、角色、裁定、消息和防误触。
- [complete] 6C. 回合反馈：等待、结果、角色和玩家积分。
- [complete] 6D. 大厅与词包：统一控件、选择器、草稿和字段反馈。
- [complete] 6E. 验收与发布：浏览器尺寸 / 键盘 / 多人流程 / 构建 / 审查 / VPS。真实手机测试如无设备须明确保留限制。

## 每批验证与合并

针对故障补回归测试；运行 npm test、npm run build、git diff --check；人工审查最终 diff 和边界场景；记录结果后提交并以 --no-ff 合并本地 main。审查发现问题在合并前修正。已有 UI 改动独立基线提交。

## 产品决定

临时会话与房间以单进程内存为准；60 秒断线宽限期；重启后旧会话明确失效。词库仍持久化到 SQLite。裁定局至少 3 人，词库局至少 2 人；服务端控制回合时限。使用稳定 roundId / guessId / strokeId 防止旧命令作用于新回合。

## 错误记录

系统 PATH 无 python；已定位并使用 Codex 自带 Python，第一次转换未执行，没有半写入。

实时集成测试首次在客户端 close 后立即读取服务端集合导致时序失败；改为等待服务端解绑状态，保留对真实异步关闭的验证。

第三批后：批量写入命令被自动审批以 blocked by policy 拦截，改用文件编辑工具完成。一次补丁同路径删除/新增被工具拒绝，拆分操作后完成。

VPS 首次安装旧 better-sqlite3 需要 g++ 而服务器没有，切换到内置 N-API 二进制的 13.0.3。一次本地验证过早在 npm install 完成前开始，出现临时缺失 binding，待安装完成后完整重跑通过。

- [in_progress] 7. 修正单字谜底提示泄露，回归两种模式，部署并将最终 main 推送 origin。
